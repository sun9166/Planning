cmake_minimum_required(VERSION 2.8.3)
project(acu_proto)

set(CROSSCOMPILE $ENV{CROSSCOMPILE})

list(APPEND PROTO_SRC_FILE Control.proto) 
list(APPEND PROTO_SRC_FILE Canbus.proto) 
list(APPEND PROTO_SRC_FILE common_msgs.proto) 
list(APPEND PROTO_SRC_FILE control_msgs.proto) 
list(APPEND PROTO_SRC_FILE mapengine_msgs.proto) 
list(APPEND PROTO_SRC_FILE perception_environment_msgs.proto) 
list(APPEND PROTO_SRC_FILE perception_objects_msgs.proto) 
list(APPEND PROTO_SRC_FILE planning_debug_msgs.proto) 
list(APPEND PROTO_SRC_FILE planning_msgs.proto) 
list(APPEND PROTO_SRC_FILE prediction_debug_msgs.proto) 
list(APPEND PROTO_SRC_FILE prediction_msgs.proto) 
list(APPEND PROTO_SRC_FILE sensor_imu_msgs.proto)  
list(APPEND PROTO_SRC_FILE vehicle_perception_msgs.proto) 
list(APPEND PROTO_SRC_FILE ControlFeedback_msgs.proto) 
list(APPEND PROTO_SRC_FILE LatcontrolDebug_msgs.proto) 
list(APPEND PROTO_SRC_FILE LoncontrolDebug_msgs.proto) 
list(APPEND PROTO_SRC_FILE Adview.proto)
list(APPEND PROTO_SRC_FILE Business.proto)

list(APPEND PROTO_SRC_FILE ccperception.proto) 
list(APPEND PROTO_SRC_FILE lidar_perception.proto) 
list(APPEND PROTO_SRC_FILE perception.proto) 
list(APPEND PROTO_SRC_FILE perception_barricade.proto) 
list(APPEND PROTO_SRC_FILE perception_diagnose.proto) 
list(APPEND PROTO_SRC_FILE perception_road.proto)  
list(APPEND PROTO_SRC_FILE perception_sensor_info.proto) 
list(APPEND PROTO_SRC_FILE perception_types.proto) 
list(APPEND PROTO_SRC_FILE perception_vehicle.proto) 
list(APPEND PROTO_SRC_FILE vehicle_info.proto) 
list(APPEND PROTO_SRC_FILE common_vehicle_info.proto) 
list(APPEND PROTO_SRC_FILE gps.proto) 
list(APPEND PROTO_SRC_FILE Vehicle2Cloud.proto) 
list(APPEND PROTO_SRC_FILE Cloud2Vehicle.proto) 



include(${CMAKE_SOURCE_DIR}/protobuf.cmake)      
